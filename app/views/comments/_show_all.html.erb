<%= link_to t(".show_all",:count=>Comment.comment_count_for(commentable)), commentable_path(commentable),:remote=>true,:id=>"show_all_comments_for_#{commentable_id(commentable)}" if Comment.comment_count_for(commentable)>1 %>